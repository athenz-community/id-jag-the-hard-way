name: Build and Push MCP Runtime Proxy

on:
  push:
    branches: [ "main" ]
    paths:
      - "components/mcp-runtime-proxy/**"
      - ".github/workflows/publish-mcp-runtime-proxy.yml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "components/mcp-runtime-proxy/**"
      - ".github/workflows/publish-mcp-runtime-proxy.yml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: mcp-runtime-proxy
  BUILD_DIR: ./components/mcp-runtime-proxy

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.DEPLOY_GHCR_SECRET || secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=pr,prefix=pr
            type=raw,value=latest,enable=${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}

      - name: Run checks
        working-directory: ${{ env.BUILD_DIR }}
        run: npm run check && npm test

      - name: Build and push the image
        uses: docker/build-push-action@v5
        with:
          context: ${{ env.BUILD_DIR }}
          file: ${{ env.BUILD_DIR }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
