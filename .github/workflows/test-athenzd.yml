name: Test athenzd

on:
  push:
    branches: ["main"]
    paths:
      - "components/athenzd/**"
      - ".github/workflows/test-athenzd.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "components/athenzd/**"
      - ".github/workflows/test-athenzd.yml"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: components/athenzd

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: components/athenzd/go.mod
          cache-dependency-path: components/athenzd/go.sum

      - name: Run tests
        run: make test
